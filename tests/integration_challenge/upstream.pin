# The IRIS-HEP integration challenge revision the CI run and the Flyte workflow
# (IC_REF in workflows/integration-challenge/workflow.py) use, pinned to a
# commit so upstream movement arrives as a Renovate PR that runs the full CI
# chain: a breaking upstream change reddens that PR while main stays green.
# The file is a memo input of the stage-1c job, so bumping it forces a re-run.

repo=https://github.com/iris-hep/integration-challenge
ref=f6cd7e1cf6801cad1a743b332c9deed073b70a00
