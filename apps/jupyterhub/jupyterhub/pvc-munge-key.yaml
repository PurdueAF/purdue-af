apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: munge-key
spec:
  storageClassName: ${multinode_storage_class}
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
