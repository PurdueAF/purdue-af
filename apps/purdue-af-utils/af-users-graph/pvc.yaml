apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: af-users-graph-pvc
  namespace: cms
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Mi
  storageClassName: geddes-standard-multinode
