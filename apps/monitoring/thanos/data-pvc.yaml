apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: thanos-data-pvc
  namespace: cms
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: geddes-standard-multinode
  resources:
    requests:
      storage: 100Gi
