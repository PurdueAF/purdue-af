# values-incluster.yaml
nodeName: interlink-incluster-node

interlink:
  enabled: true
  socket: unix:///tmp/interlink.sock

plugin:
  enabled: true
  image: "geddes-registry.rcac.purdue.edu/cms/interlink-slurm-plugin:0.5.0"
  socket: unix:///tmp/plugin.sock
  config: |
    InterlinkURL: "unix:///tmp/interlink.sock"
    SidecarURL: "unix:///tmp/plugin.sock"
    VerboseLogging: true

virtualNode:
  resources:
    CPUs: 4
    memGiB: 16
    pods: 50
