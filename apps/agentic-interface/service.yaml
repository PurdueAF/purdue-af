apiVersion: v1
kind: Service
metadata:
  name: agentic-interface
  labels:
    app: agentic-interface
    scrape-metrics: "true"
spec:
  selector:
    app: agentic-interface
  ports:
    - name: metrics
      port: 8888
      targetPort: 8888
