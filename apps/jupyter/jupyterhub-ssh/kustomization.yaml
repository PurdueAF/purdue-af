apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
configMapGenerator:
  - name: jupyterhub-ssh-config
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
