apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: cms
  name: prometheus-sa-role
rules:
  - apiGroups: [""]
    resources: ["pods", "services", "endpoints"]
    verbs: ["list", "watch"]
