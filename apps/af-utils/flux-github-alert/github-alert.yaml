apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Alert
metadata:
  name: purdue-af-alert
spec:
  providerRef:
    name: github
  eventSeverity: info
  eventSources:
    - kind: Kustomization
      name: purdue-af-flux
      namespace: ${namespace}
