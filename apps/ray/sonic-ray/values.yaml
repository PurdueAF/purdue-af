# Values for the `sonic-ray` release: Triton on Ray in the cms namespace on
# Geddes, with Ray Serve carrying its gRPC. Autoscaling is Ray's alone: no
# KEDA, no Prometheus in the loop. See the chart.
#
# The models are the ones CMSSW ships: Triton is pointed at model directories
# inside a CMSSW release on CVMFS, with an explicit load list. Nothing is
# uploaded anywhere; a new CMSSW release is a path change here.

triton:
  # Triton with the PyTorch, TensorFlow and ONNX backends plus
  # torch-geometric, so every model in the repository below has a backend to
  # run on — the image the facility's other Triton deployments already use.
  # Held at the 26.04 line by Renovate for the same reason as the plain Triton
  # image: 26.06+ is CUDA 13, whose driver floor is above the R580 on these
  # nodes.
  #
  # 7.8 GB compressed. A pod on a node that has not pulled it before waits for
  # that before Triton even starts, which is scale-up latency on top of the
  # model load — one reason replicas.min stays at 1. Pull it through
  # geddes-registry's docker-hub-cache instead if that becomes the bottleneck.
  image:
    repository: docker.io/fastml/triton-torchgeo
    tag: 26.04-py3-geometric
  command: ["/bin/sh", "-c"]
  args:
    - |
      /opt/tritonserver/bin/tritonserver \
      --http-port=8100 \
      --model-repository=/cvmfs/cms.cern.ch/el9_amd64_gcc13/cms/cmssw/CMSSW_17_0_0_pre2/external/el9_amd64_gcc13/data/RecoBTag/Combined/data/models/ \
      --model-repository=/cvmfs/cms.cern.ch/el9_amd64_gcc13/cms/cmssw/CMSSW_17_0_0_pre2/external/el9_amd64_gcc13/data/RecoEgamma/EgammaPhotonProducers/data/models/ \
      --model-repository=/cvmfs/cms.cern.ch/el9_amd64_gcc13/cms/cmssw/CMSSW_17_0_0_pre2/external/el9_amd64_gcc13/data/RecoTauTag/TrainingFiles/data/DeepTauIdSONIC/ \
      --model-repository=/cvmfs/cms.cern.ch/el9_amd64_gcc13/cms/cmssw/CMSSW_17_0_0_pre2/external/el9_amd64_gcc13/data/RecoMET/METPUSubtraction/data/models/ \
      --model-control-mode=explicit \
      --load-model=deepmet \
      --load-model=deeptau_2018v2p5 \
      --load-model=particleNetFromMiniAODAK4CHSCentral \
      --load-model=particleNetFromMiniAODAK4PuppiCentral \
      --load-model=particleNetFromMiniAODAK4PuppiForward \
      --load-model=particleNetFromMiniAODAK8 \
      --load-model=particlenet_AK8_MD-2prong_PT \
      --load-model=particlenet_AK8_MassRegression_PT \
      --load-model=particlenet_PT \
      --load-model=unifiedparticletransformer_AK4_V01 \
      --allow-gpu-metrics=true \
      --log-verbose=0 \
      --strict-model-config=false \
      --exit-timeout-secs=60
  resources:
    limits:
      nvidia.com/gpu: 1
      cpu: 4
      memory: 16G
    requests:
      nvidia.com/gpu: 1
      cpu: 4
      memory: 16G
  # Probe timings come from the chart, which carries the ones the facility's
  # other Triton deployments run. The single release-specific number: this
  # repository is loaded over the network into the node's CVMFS cache the
  # first time, which takes minutes, so the startup budget is 24 x 10s rather
  # than the chart's 12. Image pull time is not charged against it — the
  # kubelet only starts probing once the container is running.
  startupProbe:
    failureThreshold: 24
  # Triton's HTTP moves off its default 8000: the Ray container in this pod
  # binds that for Ray Serve's HTTP proxy, and containers of a pod share one
  # network namespace. gRPC keeps 8001, which is what the forwarder dials.
  httpPort: 8100
  grpcPort: 8001
  # The cluster's CVMFS claim (apps/storage/cvmfs-pvc.yaml), read-only.
  # HostToContainer: CVMFS repositories are autofs mounts on the node, and the
  # pod must see the ones mounted after it started.
  modelRepository:
    claimName: cvmfs
    mountPath: /cvmfs
    mountPropagation: HostToContainer

# One Triton idling so the first request does not wait for a pod, two image
# pulls and a model load from CVMFS; at most four, since the GPUs are shared.
replicas:
  min: 1
  max: 4

# The CMS GPU nodes, for every pod here.
nodeSelector: { "cms-af-prod": "true" }
tolerations:
  - key: hub.jupyter.org/dedicated
    operator: Equal
    value: cms-af
    effect: NoSchedule

# The private address pool: reachable from the AF, never a public ingress.
service:
  type: LoadBalancer
  annotations:
    metallb.universe.tf/address-pool: geddes-private-pool
