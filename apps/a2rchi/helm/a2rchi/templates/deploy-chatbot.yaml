apiVersion: apps/v1
kind: Deployment
metadata:
  name: a2rchi-chatbot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: a2rchi-chatbot
  template:
    metadata:
      labels:
        app: a2rchi-chatbot
    spec:
      containers:
        - name: chatbot
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          env:
            - name: PG_PASSWORD_FILE
              value: /secrets/pg_password
            {{- if .Values.model.huggingface.secretName }}
            - name: HUGGINGFACEHUB_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.model.huggingface.secretName }}
                  key: HUGGINGFACEHUB_API_TOKEN
            - name: HUGGINGFACE_HUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.model.huggingface.secretName }}
                  key: HUGGINGFACEHUB_API_TOKEN
            - name: HF_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.model.huggingface.secretName }}
                  key: HUGGINGFACEHUB_API_TOKEN
            {{- end }}
            - name: PGHOST
              value: postgres
            - name: PGPORT
              value: "5432"
            - name: PGDATABASE
              value: postgres
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: a2rchi-prompts
              mountPath: /root/A2rchi
              readOnly: true
            - name: configs-dir
              mountPath: /root/A2rchi/configs
            - name: a2rchi-config
              mountPath: /root/A2rchi/configs/purdue-af.yaml
              subPath: purdue-af.yaml
              readOnly: true
            - name: a2rchi-data
              mountPath: /root/A2rchi/data
            - name: secrets
              mountPath: /secrets
      volumes:
        - name: a2rchi-prompts
          configMap:
            name: a2rchi-config
            items:
              - key: main.prompt
                path: main.prompt
              - key: condense.prompt
                path: condense.prompt
        - name: a2rchi-config
          configMap:
            name: a2rchi-config
            items:
              - key: purdue-af.yaml
                path: purdue-af.yaml
        - name: configs-dir
          emptyDir: {}
        - name: a2rchi-data
          emptyDir: {}
        - name: secrets
          projected:
            sources:
              - secret:
                  name: a2rchi-postgres
                  items:
                    - key: POSTGRES_PASSWORD
                      path: pg_password
---
apiVersion: v1
kind: Service
metadata:
  name: a2rchi
spec:
  selector:
    app: a2rchi-chatbot
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}

