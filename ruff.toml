target-version = "py312"

[lint]
# Explicit `select`, not `extend-select`: ruff's default rule set changes between versions.
select = ["E4", "E7", "E9", "F", "I"]

[lint.per-file-ignores]
# `c` is injected by the hub at exec time.
"apps/jupyterhub/jupyterhub/extraFiles/*.py" = ["F821"]
